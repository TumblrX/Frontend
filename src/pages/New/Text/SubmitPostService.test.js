/* eslint-disable no-unused-vars */
// import submitPost from './SubmitPostService';

describe.skip('submitPost function', () => {
  it('returns false if the blogId is undefined', async () => {
    // expect(await submitPost(undefined, new FormData())).toEqual(false);
  });
});
