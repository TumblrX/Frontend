/* eslint-disable no-unused-vars */
// import checkEmail from './CheckEmailService';

describe.skip('Check that checkEmail works correctly', () => {
  it('should return true if the email is unused', async () => {
    // const response = await checkEmail('unusedEmail@example.com');
    // expect(response).toEqual(true);
  });
  it('should return false if the email is used', async () => {
    // const response = await checkEmail('example@example.com');
    // expect(response).toEqual(false);
  });
});
